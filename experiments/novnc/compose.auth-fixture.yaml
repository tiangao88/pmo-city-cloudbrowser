# ONLY combine with deploy/desktop/compose.candidate.yaml on localhost.
services:
  auth-fixture:
    image: python:3.12-slim-bookworm
    command: [python, /fixture.py]
    volumes:
      - ../../experiments/novnc/auth_fixture.py:/fixture.py:ro
    networks: [auth-egress]
