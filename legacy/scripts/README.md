# Current implementation import

The former CloudBrowser scripts are retained for migration and comparison.
They are not the new product service implementations.

The first extraction targets the router, slot supervisor, agent-control
surface, and Credential Broker boundary separately, with contract and
security tests written before moving behavior.
