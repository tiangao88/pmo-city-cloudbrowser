# Configuration model

This directory is reserved for non-secret, version-controlled configuration
schemas and examples. Runtime secrets are injected by Coolify or an approved
secret manager and must never be committed.
