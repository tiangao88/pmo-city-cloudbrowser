# CloudBrowser control contract v1

Placeholder OpenAPI seed. The lifecycle and owner-bound control contract must
be frozen before an installable release.

The final contract will define authentication, owner binding, slot lifecycle,
queue state, health, and safe error responses without exposing secrets or
unrestricted browser-control primitives.
