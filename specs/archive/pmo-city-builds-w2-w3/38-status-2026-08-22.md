# 38 — Status 2026-08-22: Cosmetic Changes, Functional Clarifications & W2 Position

> Consolidated status after the demo stop (2026-08-22, Tigo): everything
> cosmetic + every functional clarification since the last W1 audit, then
> where W2 stands against its official DoD (`20-w2-dod.md`).
> Companion: `22-w2-progress.md` (execution log, batch 2026-08-22 added).

---

## 1. Cosmetic changes (all deployed & live)

| # | Change | Surface(s) | Commit(s) |
|---|---|---|---|
| C1 | PMO City favicon (data-URI SVG) + tab title `CloudBrowser: {email}` | all 4 surfaces | `84a3bc9` |
| C2 | Landing page gets CloudBrowser top bar (Secrets · CloudFiles · email) | landing | `210ac6b` |
| C3 | **Spec 37 unified top bar LOCKED**: `[📁 CloudFiles] ⏐ [🔒 Secrets · 🔗 Shared] ⏐ <email>`; brand rule CloudBrowser = bold C+B, CloudFiles = bold C+F; Whitney font family | design | `2f4cc60` |
| C4 | Secrets+Shared are ONE block — no separator between them (exactly 2 separators total) | all | `90ee53a` |
| C5 | Unified top bar implemented on all 4 surfaces (landing, queue, session header, CloudFiles) + script renames (`router-v2-v3.py`→`router.py` etc.) | all | `e6a076c` |
| C6 | CloudFiles top bar mirrors neko header (40px `#202225`, 30px Whitney brand) | CloudFiles | `596c294` |
| C7 | Session header wordmark fix: `loudbrowser` → `loud<b>B</b>rowser` (bundle text rewrite), `font-weight:900` injection; email forwarded via router `Remote-Email` header (with `usr=` query fallback for direct-slot access) | session header | `4a14338`, `d24b154` |
| C8 | Queue page: numbered list below HR, "Active session:" label, viewer's own email always listed (others gated by `CB_QUEUE_SHOW_EMAILS`) | queue | `b14cc1b`, `4e63518` |
| C9 | CloudBrowser pill in CloudFiles top bar: non-breaking space between 🌐 and wordmark (no mid-pill line break) | CloudFiles | `78decd1` |

## 2. Functional clarifications (all deployed & live)

| # | Change | Behaviour | Commit(s) |
|---|---|---|---|
| F1 | Time-based queue ETA | ETA counts down from active sessions' remaining time (was a static median estimate); cold start = half the max session | `33f4c3f` |
| F2 | Queue id persistence | `queue_seq` persisted — no duplicate `q-1` after router restarts | `860282d` |
| F3 | Active reload enters session directly | `GET /` for an active user → 302 straight into `/?pwd=&usr=`; session clock refreshed only when stale or future-skewed; watchdog self-heals a dropped WebSocket (no more neko LOG IN screen) | `1e23a49`, `6aaa59f`, `acdc360` |
| F4 | Offer grace period | Freed slot offered to type-aware queue head (60 s, `CB_OFFER_GRACE_S`); Open Browser click starts the session clock at take-over; lapse → back of queue, archive `offer_expired`; zombie-button re-hide guard | `a6b8ef0` |
| F5 | Live countdowns replace "?" | Position slot shows mm:ss: offered → "Offer expires in", active → "Session ends in", waiting → "Your position" + number; `/queue/status` exposes `offer_ttl_s` / `session_ttl_s` | `26f6f0c` |
| F6 | Lapsed offer re-renders | Poll NEVER stops after Open Browser shows (removed `clearInterval`): a lapsed offer re-renders "Your position" + new position + hides the dead button — no more freeze at 0:00. Regression test `render-lapse.py` (+ negative control reproducing the old freeze) | `eb7b6a6` |
| F7 | Human session limit 30 → 15 min | `CB_HUMAN_MAX_SESSION_MIN` 15 in Coolify env; redeployed; running sessions count elapsed against the new ceiling | `f2b6ae0` |
| F8 | CloudFiles never queues | `cloudfiles.…` host routes to the files API directly — no slot acquisition, no wake, no queue page (files consume no CPU, always reachable) | `5f7deba` |
| F9 | Session-end UX | Watchdog injected into neko index redirects expired sessions to the queue page; `/fleet/my-status` read-only endpoint; production timeouts `IDLE_TIMEOUT_MIN=20` / `IDLE_GRACE_MIN=5` | `acdc360` |

**Verification state (2026-08-22):** harness 43/43; `render-offer.py` PASS (countdown ticks 1:00→0:58); `render-lapse.py` PASS + negative control FAIL (proves the test catches the freeze); all changes live-verified on the fleet (slot-1 + slot-2 served HTML checked; router in-container greps).

## 3. W2 position (official DoD, `20-w2-dod.md` — 14 items)

> **Verification note (2026-08-22):** this table was cross-checked against
> the official DoD (`20-w2-dod.md`), the execution log
> (`22-w2-progress.md`) and the results docs (`35-d10-results.md`,
> `36-spec31-results.md`). The DoD checkboxes were stale (D4–D12 done
> items still `[ ]`); they have been updated to match the recorded
> evidence — 20/26 boxes checked, remaining open = D1 sign-off items,
> D2, D3, D9-soak, D13, D14.

| Item | Status | Evidence / note |
|---|---|---|
| D1 Per-user identity & isolation | 🟡 **implemented — pending sign-off** | per-user auto-login links (`?pwd=…&usr=<email>`), queue assigns slots per identity, SSO is the only gate (neko login form removed). Static `NEKO_PASSWORD` still present as the auto-login parameter — rotation pending; needs 2-pilot confirmation + tester names |
| D2 Hybrid 2FA | ⏸ **blocked — needs inputs** | requires Vaultwarden test items (ask A2) before TOTP autonomous-vs-ask path can be built/tested |
| D3 Share-vault + deterministic broker | 🟡 **design-locked — implementation pending** | extension approach DROPPED (final, Tigo 08-21); broker-only + GrantHub per-user revocable grant (`34-granthub.md`); Phase A (sso-broker) proven; Phase B/C + OIDC flow remain |
| D4 Restart-Chrome button | ✅ done | HTTP → supervisorctl, <30 s, profile intact; janitor CDP watchdog (Batch 1) |
| D5 Tab persistence | ✅ done | agent-managed tab snapshot; restore on any restart incl. container recreate (Batch 1) |
| D6 browser-use re-validation | ✅ done | decision (Tigo): local vendored driver `pmoc_cb.py` (skill `pmoc-cdp-cloudbrowser`), NOT Hermes `browser_exec`; downloads + tab-switch ops covered |
| D7 Canvas fit / kiosk | ✅ done | `NEKO_SCREEN`-derived window, CfT notice solved, footers/pagination visible, tab bar bottom default (Batch 1) |
| D8 Downloads UX | ✅ done | CloudFiles page (in-viewer list + agent retrieval), flat 5 GB/90 d, ClamAV at ingest, EICAR quarantined-not-deleted (Batch 2) |
| D9 Capacity limits + soak | 🔄 **soak running** | limits applied (per-slot ≈0.5 CPU / 2 GB); 3-day soak on cb-fleet-v2 restarting 08-21 ~14:30 UTC → verdict ≈08-24 14:30 UTC; soak-checker repaired 08-21 (was blind: template RC 64 → empty IRs) |
| D10 Denial path | ✅ done 2026-08-21 | non-member `spike-user2` denied both domains (Authentik page, HTTP 200 = clean denial per amended DoD); member regression ✅ (`35-d10-results.md`) |
| D11 Tooling image + drift pin | ✅ done | xdotool/curl/jq after recreate; CfT pinned across watchtower cycles (Batch 2) |
| D12 Viewer hygiene | ✅ done | no Translate popup, no stale Bitwarden popups (Batch 1) |
| D13 Screen-follow | ⏸ **blocked — A7 decision** | canvas-resizes-to-window design ready; needs Tigo's A7 choice |
| D14 SME workflow validation | ⏸ **blocked — needs testers** | top-3 CRM workflows by tester + agent; requires tester names / session |

**Tallies:** 9 ✅ done · 2 🟡 implemented/design-locked (D1, D3) · 1 🔄 in progress (D9 soak) · 3 ⏸ blocked on Tigo inputs (D2, D13, D14).

## 4. Live fleet state (2026-08-22, after session-limit change)

- All 5 containers healthy (router, slot-1, slot-2, janitor, clamav) after redeploy; router env `CB_HUMAN_MAX_SESSION_MIN=15` verified.
- Persisted state survives redeploys: queue intact (`spike-user` waiting, `montigaud` offered at snapshot time).
- Sessions started before the 15-min change count elapsed time against the new ceiling (reaper expires at ≥15 min elapsed).

## 5. Open / next actions

1. **D9 soak verdict** ≈ 2026-08-24 14:30 UTC (cron `c4c83dd2eb84` daily 06:00 UTC → pmoc_HA01).
2. **Tigo inputs needed:** tester names (D1/D14), Vaultwarden test items (D2), A7 screen-follow decision (D13), GrantHub spec 34 design review (D3).
3. Healthchecks on fleet slots (Tigo mandate, non-blocking — spec 29 §11).
