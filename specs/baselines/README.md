# Specification baselines

Approved specifications are copied here as immutable snapshots named by the
product release, for example `v0.2.0/`. The bootstrap does not create a
baseline because no installable CloudBrowser release has yet been approved.

A baseline must include its requirement set, security model, contracts,
traceability, migration notes, and approval record.
